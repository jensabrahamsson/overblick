# Överblick — core dependencies
# Install: pip install -r requirements.txt
# For development: pip install -r requirements-dev.txt

aiohttp>=3.9
feedparser>=6.0
pydantic>=2.0
pyyaml>=6.0
rapidfuzz>=3.0
cryptography>=42.0
keyring>=25.0
