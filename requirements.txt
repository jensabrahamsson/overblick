# Överblick — core dependencies
# Install: pip install -r requirements.txt
# For development: pip install -r requirements-dev.txt

aiohttp>=3.9
aiohttp-socks>=0.9  # SOCKS5 proxy support for egress pool (VPS + Claude2)
feedparser>=6.0
pydantic>=2.0
pyyaml>=6.0
rapidfuzz>=3.0
cryptography>=42.0
keyring>=25.0
web3>=6.0
eth-account>=0.5
py-clob-client>=0.34
